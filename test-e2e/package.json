{
  "name": "cypress-fail-fast-e2e-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "serve -l 3000",
    "test": "jest --runInBand --verbose",
    "test:build": "cross-env node commands/build.js",
    "test:install": "cd cypress-variants/cypress-6 && npm i && cd ../cypress-7 && npm i && cd ../typescript && npm i && cd ../cypress-8 && npm i && cd ../cypress-9 && npm i && cd ../cypress-10 && npm i",
    "serve-and-test": "start-server-and-test serve http-get://localhost:3000 test",
    "test:ci": "npm run test:build && npm run test:install && npm run serve-and-test"
  },
  "devDependencies": {
    "cross-env": "7.0.3",
    "fs-extra": "10.1.0",
    "jest": "27.5.1",
    "serve": "13.0.4",
    "start-server-and-test": "1.14.0",
    "strip-ansi": "6.0.0"
  }
}
